<% content_for :right_column do %>
  <%= render 'comfy/admin/cms/sites/mirrors' %>
<% end %>
<div id="cms-uploader">
  <div id="cms-uploader-drag-drop-target"></div>
  <div class="page-header">
    <a class="btn btn-default pull-right" href="<%= new_admin_partner_path %>">Create new partner</a>
    <h2>Partners</h2>
  </div>
  <%= comfy_paginate @partners %>
  <ul class="list sortable">
    <%= render :partial => 'partner', :collection => @partners %>
  </ul>
  <%= comfy_paginate @partners %>
</div>
