<% content_for :right_column do %>
  <%= render 'comfy/admin/cms/sites/mirrors' %>
<% end %>
<div id="cms-uploader">
  <div id="cms-uploader-drag-drop-target"></div>
  <div class="page-header">
    <a class="btn btn-default pull-right" href="<%= new_admin_theme_path %>">Create new theme</a>
    <h2>Themes</h2>
  </div>
  <%= comfy_paginate @themes %>
  <ul class="list sortable">
    <%= render :partial => 'theme', :collection => @themes %>
  </ul>
  <%= comfy_paginate @themes %>
</div>
