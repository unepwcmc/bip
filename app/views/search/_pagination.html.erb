<%= render partial: "components/pagination", locals: {
  page_index: @page_index,
  current_page: @current_page,
  per_page: @per_page,
  total_pages: @total_pages,
  total_results: @total_results,
  from: @from,
  to: @to
} %>

